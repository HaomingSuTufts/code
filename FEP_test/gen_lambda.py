# Read in arguments from the command line and generate series of gromacs input files for lambda windows as input for the alchemical free energy calculation

import os
import sys
import argparse

def main(args):

    if len()
